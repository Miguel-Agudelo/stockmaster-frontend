/**
 * Constantes de roles del sistema.
 * Usar ROLES en lugar de strings literales en todo el codigo.
 */
const ROLES = {
    ADMIN:    'ADMINISTRADOR',
    OPERATOR: 'OPERADOR',
};

export default ROLES;